@model IEnumerable<GestionEcole.Models.TypeEvaluation>

@{
    ViewBag.Title = "Les Types d'Evaluations";
}

<h2>@ViewBag.Title</h2>

<p>
    <a class="btn btn-default" href="/TypeEvaluation/Create"><span class="glyphicon glyphicon-plus"></span></a>   
</p>
<div class="col-xs-8 col-xs-offset-2">
    <table class="table table-striped table-responsive">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Type)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Ponderation)
                </th>
                <th></th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Type)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Ponderation)
                    </td>
                    <td>
                        <div class="btn-group">
                            <a class="btn btn-default" href="/TypeEvaluation/Edit?id=@item.TypeEvaluationId"><span class="glyphicon glyphicon-pencil"></span></a>
                            <a class="btn btn-default" href="/TypeEvaluation/Details?id=@item.TypeEvaluationId"><span class="glyphicon glyphicon-list"></span></a>
                            <a class="btn btn-default" href="/TypeEvaluation/Delete?id=@item.TypeEvaluationId"><span class="glyphicon glyphicon-trash"></span></a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>

    </table>
</div>